var db = require('../db');

module.exports = {
  getAll: function () {}, // a function which produces all the messages, connect to db
  create: function () {} // a function which can be used to insert a message into the database
};
